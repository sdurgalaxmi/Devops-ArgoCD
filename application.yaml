apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/ramannkhanna2/argocd-repo.git
    targetRevision: HEAD
    path: dev
  destination: 
#   server: https://kubernetes.default.svc
    server: https://172.31.7.67:6443
    namespace: myapp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
